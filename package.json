{
  "name": "rally-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Rally hit-timer tracker for alliance/castle battles. Static app, no build step required.",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "serve": "npx --yes http-server -c-1 . -p 8080"
  }
}
